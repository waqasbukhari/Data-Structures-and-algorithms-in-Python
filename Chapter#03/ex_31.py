"""
S = n element seq

even entries run in O(n) time
odd entries run in O(log n) time

worst case would be there are all even entries in
S. So worst case runtime is O(n^2)

best case would be that there re only odd elements in S.
So best case run time is O(log n)



"""
